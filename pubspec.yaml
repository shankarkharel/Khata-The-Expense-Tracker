name: Khata
description: A app that lets you track your Expenses.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  fl_chart: ^0.36.2
  flutter:
    sdk: flutter
  flutter_riverpod: ^0.14.0+3
  lint: ^1.5.3
  shared_preferences: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:


  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
